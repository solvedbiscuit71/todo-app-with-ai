## Todo List (services)

This directory contains the backend source code, built using Python and FastAPI. It is recommended to use python virtual environment
for setup.

```bash
python3 -m venv .venv
pip install -r requirements.txt
```